# HTCondor submission for the tower-based inner-R scan on purity MC closure.
# Runs oneforall_tree_and_yield.sh (RecoEffCalculator + MergeSim + CalculatePhotonYield)
# for each of the 8 tower inner-R scan configs.

universe              = vanilla

PeriodicHold          = (NumJobStarts>=1 && JobStatus == 1)
Notification          = Never

executable            = /bin/bash
arguments             = "oneforall_tree_and_yield.sh $(cfg)"

request_memory        = 8GB
request_disk          = 10GB
request_cpus          = 1

log                   = logs/$(cfg).log
output                = logs/$(cfg).out
error                 = logs/$(cfg).err

# Queue one job per tower inner-R scan config (8 total)
queue cfg matching (config_bdt_innerRtower_*.yaml)
